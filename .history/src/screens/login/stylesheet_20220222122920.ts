import {
    StyleSheet
} from 'react-native';

const stylesheet = StyleSheet.create({
    container: {
        padding: 20,
        flex: 1
    },
    textInput: {
        marginBottom: 10
    }
});
export default stylesheet;
