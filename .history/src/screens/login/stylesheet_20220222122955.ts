import {
    StyleSheet
} from 'react-native';

const stylesheet = StyleSheet.create({
    container: {
        padding: 20,
        flex: 1
    },
    textInput: {
        marginBottom: 10,
        borderWidth: 2,
        borderColor: "#d3d3d3"
    }
});
export default stylesheet;
