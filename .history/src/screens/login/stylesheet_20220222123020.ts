import {
    StyleSheet
} from 'react-native';

const stylesheet = StyleSheet.create({
    container: {
        padding: 20,
        flex: 1
    },
    textInput: {
        borderColor: "#d3d3d3",
        marginBottom: 10,
        borderRadius: 8,
        borderWidth: 1
    }
});
export default stylesheet;
