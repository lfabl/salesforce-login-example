import {
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    container: {
        justifyContent: "center",
        paddingHorizontal: 40,
        flexDirection: "row",
        alignItems: "center",
        paddingVertical: 12,
        borderRadius: 50
    },
    title: {
        fontWeight: "600",
        fontSize: 16
    }
});
export default styles;