import {
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    container: {
        justifyContent: "center",
        alignItems: "center",
        padding: 20,
        flex: 1
    },
    content: {
        fontWeight: "500",
        color: "black",
        fontSize: 18
    },
    image: {
        maxHeight: 300
    },
    item: {
        marginBottom: 20
    }
});
export default styles;